autonumber

aggregate -> MysqlClient : select

autonumber stop
aggregate <-- MysqlClient

autonumber resume
aggregate -> MysqlClient : select

autonumber stop
aggregate <-- MysqlClient

loop
  autonumber resume
  create Feature
  aggregate -> Feature : new
  aggregate -> Feature : save!
end

loop
  aggregate -> Race : find

  autonumber stop
  aggregate <-- Race

  autonumber resume
  aggregate -> Feature : update!
  aggregate -> Entry : find

  autonumber stop
  aggregate <-- Entry

  autonumber resume
  aggregate -> Feature : update!
  aggregate -> Result : find

  autonumber stop
  aggregate <-- Result

  autonumber resume
  aggregate -> Feature : update!
end
