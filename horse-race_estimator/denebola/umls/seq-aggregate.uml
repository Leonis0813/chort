autonumber

aggregate -> Race : pluck

autonumber stop
aggregate <-- Race

autonumber resume
aggregate -> Feature : pluck

autonumber stop
aggregate <-- Feature

loop
  autonumber resume
  aggregate -> Feature : new

  aggregate -> Race : find

  autonumber stop
  aggregate <-- Race

  autonumber resume
  aggregate -> Entry : find

  autonumber stop
  aggregate <-- Entry

  autonumber resume
  aggregate -> Result : find

  autonumber stop
  aggregate <-- Result

  autonumber resume
  aggregate -> Feature : save!
end
